.next
node_modules
out
dist

/src/generated/prisma

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# Local device ingest config (may contain MQTT passwords)
config/energy-device-ingest.json

# Local backups and logs
backups/
*.log
scripts/_*.tmp
