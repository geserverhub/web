.next
node_modules
out
dist

/src/generated/prisma

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# Backup files (too large for git)
backups/
*.tar.gz
*.sql
